import { TracebackOption } from './index.d';

const VERSION = '1.0.0';
const DEFAULT_OPTS: TracebackOption = {
    start: 0,
};
const a = 1;


/**
 * 
 * @param selectors DOM 选择符，同 `querySelector`
 * @param opts 配置对象
 */
function init(selectors: string, opts: TracebackOption = DEFAULT_OPTS) {

}

export default {
    version: VERSION,
};
